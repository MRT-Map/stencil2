name: Publish
on:
  workflow_dispatch:
  push:
    tags: ["**"]

env:
  CARGO_TERM_COLOR: always

jobs:
  version-changelog:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      changelog: ${{ steps.changelog.outputs.changelog }}
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: true
      - run: cargo update
      - id: version
        run: echo "version=$(cargo pkgid | cut -d '#' -f 2)" >> "$GITHUB_OUTPUT"
      - id: changelog
        run: |
          {
          echo 'changelog<<__EOF__'
          python -c "
          import os
          print('\n'.join(open('CHANGELOG.md').read().split('## v' + os.environ['VERSION'])[1].split('\n')[1:]).split('\n##')[0])
          "
          echo __EOF__
          } >> "$GITHUB_OUTPUT"
        env:
          VERSION: ${{ steps.version.outputs.version }}

  build-macos-aarch64:
    runs-on: macos-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: true
      - uses: ./.github/actions/mise
        with:
          cache: false
          envs: "publish"

      - run: cargo --locked bundle --release
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: macos-aarch64
          path: target/release/bundle/osx/stencil3.app

  build-macos-x86_64:
    runs-on: macos-26-intel
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: true
      - uses: ./.github/actions/mise
        with:
          cache: false
          envs: ""

      - run: cargo --locked build --release
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: macos-x86_64
          path: target/release/stencil3

  build-macos:
    runs-on: macos-latest
    needs: [build-macos-aarch64, build-macos-x86_64, version-changelog]
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: true
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos-aarch64
          path: stencil3.app
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos-x86_64
      - run: brew install create-dmg

      - run: |
          lipo stencil3.app/Contents/MacOS/stencil3 stencil3 -create -output stencil3-new
          mv stencil3-new stencil3.app/Contents/MacOS/stencil3
      - run: |
          create-dmg \
            --volname stencil3 \
            --volicon "stencil3.app/Contents/Resources/stencil3.icns" \
            --background "assets/ste-dmg-background.png" \
            --window-size 600 325 \
            --icon-size 100 \
            --icon "stencil3.app" 200 180 \
            --hide-extension "stencil3.app" \
            --app-drop-link 400 180 \
            --eula LICENSE \
            "stencil3-$VERSION.dmg" "stencil3.app"
        env:
          VERSION: ${{ needs.version-changelog.outputs.version }}

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: macos
          path: stencil3-*.dmg

  build-linux:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm]
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: true
      - uses: ./.github/actions/mise
        with:
          cache: false
          envs: "publish"
      #    - uses: awalsh128/cache-apt-pkgs-action@acb598e5ddbc6f68a970c5da0688d2f3a9f04d05 # v1.6.0
      #      with:
      #        packages: libasound2-dev libudev-dev libwayland-dev libxkbcommon-dev
      #        version: 1.0

      - run: cargo --locked bundle --release --format deb
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: linux-${{ matrix.os }}
          path: target/release/bundle/deb/stencil3*.deb

  build-windows:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, windows-11-arm]
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: true
      - uses: ./.github/actions/mise
        with:
          cache: false
          envs: "publish"

      - run: cargo --locked build --release
      - run: cargo wix --toolset modern -I "./build/windows/main.wxs" -v --nocapture --package stencil3
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: windows-${{ matrix.os }}
          path: target/wix/stencil3-*.msi

  github-release:
    runs-on: ubuntu-slim
    needs: [build-linux, build-macos, build-windows, version-changelog]
    permissions:
      id-token: write
      attestations: write
      contents: write

    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: linux-ubuntu-latest
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: linux-ubuntu-24.04-arm
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: windows-windows-latest
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: windows-windows-11-arm
          path: dist

      - uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: dist/*
      - uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2  # zizmor: ignore[superfluous-actions]
        with:
          tag_name: v${{ needs.version-changelog.outputs.version }}
          name: v${{ needs.version-changelog.outputs.version }}
          body: ${{ needs.version-changelog.outputs.changelog }}
          files: dist/*
          prerelease: true
          generate_release_notes: true

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: combined-dist
          path: dist/*

  delete-github-release:
    runs-on: ubuntu-slim
    needs:
      [
        build-macos-aarch64,
        build-macos-x86_64,
        build-macos,
        build-linux,
        build-windows,
        github-release,
        version-changelog,
      ]
    if: ${{ always() && contains(needs.*.result, 'failure') }}
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: true
      - run: gh release delete "v$TAG" --cleanup-tag -y
        env:
          TAG: ${{ needs.version-changelog.outputs.version }}
          GH_TOKEN: ${{ github.token }}
        continue-on-error: true

  update-hash:
    runs-on: ubuntu-slim
    needs: [github-release, version-changelog]
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: master
          persist-credentials: true
          submodules: true
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: combined-dist
          path: dist

      - env:
          VERSION: ${{ needs.version-changelog.outputs.version }}
        run: |
          MAC_SHA=$(shasum -a 256 "dist/stencil3-${VERSION}.dmg" | cut -d ' ' -f1)
          sed "s/sha256 \".*\"/sha256 \"$MAC_SHA\"/" -i build/macos/brew.rb
          sed "s/version \".*\"/version \"$VERSION\"/" -i build/macos/brew.rb

          LINUX_SHA1=$(shasum -a 256 "dist/stencil3_${VERSION}_amd64.deb" | cut -d ' ' -f1)
          LINUX_SHA2=$(shasum -a 256 "dist/stencil3_${VERSION}_arm64.deb" | cut -d ' ' -f1)
          sed "s/sha256sums_x86_64=.*/sha256sums_x86_64=('$LINUX_SHA1')/" -i build/linux/PKGBUILD
          sed "s/sha256sums_aarch64=.*/sha256sums_aarch64=('$LINUX_SHA2')/" -i build/linux/PKGBUILD
          sed "s/pkgver=.*/pkgver=${VERSION//-/_}/" -i build/linux/PKGBUILD

          sed "s/\"version\": .*/\"version\": \"$VERSION\",/" > build/windows/scoop.json
      - uses: EndBug/add-and-commit@290ea2c423ad77ca9c62ae0f5b224379612c0321 # v10.0.0
        with:
          default_author: github_actions
          message: update sha256 for v${{ needs.version-changelog.outputs.version }}

  trigger-pkg-manager-updates:
    uses: ./.github/workflows/trigger-pkg-manager-updates.yml
    needs: update-hash
    permissions:
      contents: read
    secrets:
      PAT: ${{ secrets.PAT }}
