# Maintainer: 7d <i.third.7d@protonmail.com>
pkgname=stencil2
pkgver=2.1.0
pkgrel=1
depends=('curl')
makedepends=('rustup')
arch=('x86_64' 'aarch64' 'armv7')
pkgdesc="Map editor for MRT Map data"
license=('GPL3')
url="https://github.com/mrt-map/stencil2"
source=("$pkgname-$pkgver.tar.gz::$url/archive/v$pkgver.tar.gz")
sha256sums=('7c67d6133109c2347ad1984c06dbd04c7fe6f09a3b5e8eb19c9c00eee6c11930')

build() {
  cd "$pkgname-$pkgver"
  rustup toolchain install nightly
  cargo +nightly build --release
}

package() {
  cd "$pkgname-$pkgver"
  install -Dm755 target/release/$pkgname "$pkgdir/usr/bin/$pkgname"
  install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}

post_remove() {
    rm -r "~/.local/share/stencil2"
}
